{
  "name": "electricalsetup",
  "version": "0.0.0",
  "private": true,
  "description": "Preliminary Setup Universe wrapper for ElectricalSetup.",
  "homepage": "https://electricalsetup.com/",
  "scripts": {
    "prototype:test": "npm --prefix prototype test",
    "prototype:check": "npm --prefix prototype run check",
    "prototype:serve": "npm --prefix prototype run serve"
  }
}
