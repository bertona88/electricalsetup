{
  "name": "electricalsetup",
  "version": "0.1.0",
  "private": true,
  "description": "A deterministic causal electrical laboratory for the browser.",
  "homepage": "https://electricalsetup.com/",
  "type": "module",
  "scripts": {
    "build": "npm run build:ts && npm run build:wasm",
    "build:ts": "tsc",
    "build:wasm": "node tests/build-wasm.mjs",
    "test": "npm run build && node tests/smoke.mjs",
    "serve": "python3 -m http.server 4173 --directory app"
  },
  "devDependencies": {
    "@webgpu/types": "^0.1.64",
    "typescript": "^5.9.2",
    "wabt": "^1.0.37"
  }
}
